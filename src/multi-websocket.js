/**
 * Encapsulates the different objects that compose the MultiWebsocket library.

 * @namespace MultiWebsocket
 */

'use strict';

module.exports = {
  EventBus: require('./event-bus'),
  Client: require('./client')
};
